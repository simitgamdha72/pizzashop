@* @model pizzashop.Models.ViewModels.RoleViewModel; *@
<div id="rolesPageContent" class="container-fluid px-3 py-4 p-md-5">

    <div class="bg-white p-5">
        <div class="d-flex justify-content-between align-items-center fs-5 fw-semibold" style="color: #727b83;">
            <div class="">Role</div>
            <div class="">Action</div>
        </div>
        <hr>
        <div class="d-flex justify-content-between align-items-center">
            <div class="">Admin</div>
            <a class="actionBtn px-2 py-1" type="button"
                href="@Url.Action("permission", "RoleandPermission", new { id = 1 })"><i class="bi bi-pen"></i></a>
        </div>
        <hr>
        <div class="d-flex justify-content-between align-items-center">
            <div class="">Account Manager</div>
            <a class="actionBtn px-2 py-1" type="button"
                href="@Url.Action("permission", "RoleandPermission", new { id = 2 })"><i class="bi bi-pen"></i></a>
        </div>
        <hr>
        <div class="d-flex justify-content-between align-items-center">
            <div class="">Chef</div>
            <a class="actionBtn px-2 py-1" type="button"
                href="@Url.Action("permission", "RoleandPermission", new { id = 3 })"><i class="bi bi-pen"></i></a>
        </div>
    </div>
</div>


@* 
@model pizzashop.Models.ViewModels.RoleViewModel;

<div id="rolesPageContent" class="container-fluid px-3 py-4 p-md-5">
    <div class="bg-white p-5">
        <div class="d-flex justify-content-between align-items-center fs-5 fw-semibold" style="color: #727b83;">
            <div class="">Role</div>
            <div class="">Action</div>
        </div>
        <hr>

        <!-- Assuming you have a list of roles, loop through them -->
        @foreach (var role in Model.Role)
        {
            <div class="d-flex justify-content-between align-items-center">
                <div class="">@role.Role1</div>
                <a class="actionBtn px-2 py-1" type="button"
                    href="@Url.Action("permission", "RoleandPermission", new { id = role.Id })">
                    <i class="bi bi-pen"></i>
                </a>
            </div>
            <hr>
        }
    </div>
</div> *@
