@model pizzashop.Models.ViewModels.CategoryViewModel

<div id="menu" class="container-fluid">

    <div class="fs-2 fw-semibold mb-3" style="color: #006CAC;">Menu</div>

    <!-- menuNavbar -->
    <div class="menubar mb-2 mb-sm-3 rounded-1 bg-white">
        <ul class="nav">
            <li class="active">
                <a class="nav-link">
                    <i class="bi bi-list-check me-2"></i>
                    Items
                </a>
            </li>
            <li>
                <a class="nav-link">
                    <i class="bi bi-ui-checks-grid me-2"></i>
                    Modifiers
                </a>
            </li>
        </ul>
    </div>

    <div class="d-flex">


        <!-- Categories Section -->
        <div class="offcanvas-lg start-0" data-bs-scroll="true" tabindex="-1" id="offcanvascategory">
            <div class="bg-light min-vh-100" style="width: 200px;display:flex;flex-direction: column;">
                <div class="justify-content-between w-100 p-3" style="display: flex;align-items: center;">
                    <div class="fs-4 fw-semibold" style="color: #006CAC;">Category</div>
                    <button class="customBtnSecondary fs-4 py-0 px-2" data-bs-toggle="modal"
                        data-bs-target="#addCategoryModal"><i class="bi bi-plus-lg"></i></button>
                </div>
                <ul class="categorys flex-column mb-auto w-100" style="overflow-y: scroll; max-height: 400px;">



                    @{
                        foreach (var Categories in Model.categories)
                        {
                            <li class="active">

                                @* <a href="@Url.Action("menu","Category",new {id = @Categories.Id})"
                                    data-category-id="@Categories.Id" class="nav-link d-flex me-auto">
                                    <i class="bi bi-grid-1x2-fill" style="margin-right: 10px;"></i>
                                    @Categories.Category1
                                </a> *@
                                <a href="javascript:void(0);" data-category-id="@Categories.Id" class="nav-link d-flex me-auto">
                                    <i class="bi bi-grid-1x2-fill" style="margin-right: 10px;"></i>
                                    @Categories.Category1
                                </a>
                                <div class="categoryAction">
                                    <div class="d-flex">
                                        <button class="menubtn" data-bs-toggle="modal" data-bs-target="#editCategoryModal"
                                            data-id="@Categories.Id" data-category-name="@Categories.Category1"
                                            data-description="@Categories.Description">
                                            <i class="bi bi-pen"></i>
                                        </button>
                                        <!-- Delete button with data-id attribute to pass CategoryId -->
                                        <button class="menubtn" data-bs-toggle="modal" data-bs-target="#delete"
                                            data-id="@Categories.Id">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </div>
                                </div>
                            </li>

                            <!-- Delete Category Modal -->
                            <div class="modal fade" style="margin-top: 13%;" id="delete" tabindex="-1"
                                aria-labelledby="exampleModalLabel" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h3 class="modal-title fs-5" id="exampleModalLabel">Delete Confirmation</h3>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                aria-label="Close"></button>
                                        </div>
                                        <div class="modal-body"
                                            style="display: flex;flex-direction: column; align-items: center;justify-content: center;">
                                            <div style="color: #ffc409;">
                                                <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40"
                                                    fill="currentColor" class="bi bi-exclamation-triangle-fill"
                                                    viewBox="0 0 16 16">
                                                    <path
                                                        d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5m.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2" />
                                                </svg>
                                            </div>
                                            <div>
                                                <p>Are you sure you want to delete this category?</p>
                                            </div>
                                            <div class="modal-footer">
                                                <!-- The form where the CategoryId will be passed -->
                                                <form id="deleteForm" method="post" asp-controller="Category"
                                                    asp-action="deletecategory">
                                                    <input type="hidden" id="categoryId" name="id" />
                                                    <button type="submit" class="btn btn-primary">Yes</button>
                                                    <button type="button" class="btn btn-secondary"
                                                        data-bs-dismiss="modal">No</button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Modal for Editing Category -->
                            <div class="modal fade" style="margin-top: 13%;" id="editCategoryModal" tabindex="-1"
                                aria-labelledby="editCategoryModallable" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h1 class="modal-title fs-5" id="exampleModalLabel">Edit Category</h1>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                aria-label="Close"></button>
                                        </div>
                                        <div class="modal-body">
                                            <div class="form">
                                                <form asp-controller="Category" asp-action="EditCategory" method="post">
                                                    <!-- Hidden input to pass CategoryId -->
                                                    <input type="hidden" id="CategoryId" name="Id" />

                                                    <div class="mb-3">
                                                        <label for="CategoryName" class="form-label">Category Name</label>
                                                        <input type="text" class="form-control" id="CategoryName"
                                                            name="Category1" placeholder="Name">
                                                    </div>

                                                    <div class="mb-3">
                                                        <label for="Description" class="form-label">Description</label>
                                                        <input type="text" class="form-control" id="Description"
                                                            name="Description" placeholder="Description">
                                                    </div>

                                                    <div class="modal-footer">
                                                        <button type="submit" class="btn btn-primary">Save</button>
                                                        <button type="button" class="btn btn-secondary"
                                                            data-bs-dismiss="modal">Cancel</button>
                                                    </div>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        }
                    }


                </ul>



            </div>
        </div>


        <!-- Menu Items -->
        <div class="bg-white container-fluid py-3 px-3">
            <div style="display: flex; align-items:center;">
                <button class="d-lg-none fs-4 me-2 menubtn" style="color: #006CAC;" type="button"
                    data-bs-toggle="offcanvas" data-bs-target="#offcanvascategory" aria-controls="offcanvascategory">
                    <i class="bi bi-list-ul"></i>
                </button>
                <div class="fs-4 fw-semibold" style="color: #006CAC;">Items</div>
            </div>
            <div class="mb-2 mb-sm-3" style="display: flex; justify-content: end; align-items: center;">
                <div class="d-flex">
                    <form class="searchBar d-none d-sm-flex me-3">
                        <input class="form-control" type="text" placeholder="Search">
                        <i class="searchIcon bi bi-search"></i>
                    </form>
                    <button class="customBtnSecondary px-md-3 me-3 fs-4" href="addNewUser.html"><i
                            class="bi bi-trash"></i></button>
                    @* <button class="customBtnPrimary px-3 p-md-3" data-bs-toggle="modal"
                        data-bs-target="#addItemModal"><i class="bi bi-plus-lg me-0 me-sm-2"></i><span
                            class="d-none d-sm-block">New Item</span></button> *@
                    <button class="customBtnPrimary px-3 p-md-3" id="loadModalBtn"><i
                            class="bi bi-plus-lg me-0 me-sm-2"></i><span class="d-none d-sm-block">New
                            Item</span></button>
                    @* <button id="loadModalBtn" class="btn btn-primary">Add New Item</button> *@
                </div>

            </div>
            <div class="d-block d-sm-none mb-3">
                <form class="searchBar">
                    <input class="form-control" type="text" placeholder="Search">
                    <i class="searchIcon bi bi-search z-3"></i>
                </form>
            </div>

            <div class="">
                @* <div class="table-responsive">
                    <table class="tableList table align-middle">
                        <thead>
                            <tr>
                                <th scope="col">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" value="" id="selectAllCheckbox">
                                    </div>
                                </th>
                                <th scope="col">Name</th>
                                <th scope="col">ItemType</th>
                                <th scope="col">Rate</th>
                                <th scope="col">Quantity</th>
                                <th scope="col">Available</th>
                                <th scope="col">Action</th>
                            </tr>
                        </thead>
                        <tbody class="table-group-divider">

                            @{
                                foreach (var menuitems in Model.menuItems)
                                {
                                    <tr>
                                        <td>
                                            <div class="form-check">
                                                <input class="form-check-input checkboxes" type="checkbox" value=""
                                                    id="flexCheckDefault">
                                            </div>
                                        </td>
                                        <td>
                                            <div class="listImgField">
                                                <img class="listImg" src="~/images/dining-menu.png">
                                                @menuitems.Name
                                            </div>
                                        </td>
                                        <td>
                                            @if (menuitems.ItemType == 1)
                                            {
                                                <img src="~/images/icons/veg-icon.svg" height="20px">
                                            }
                                            @if (menuitems.ItemType == 2)
                                            {
                                                <img src="~/images/icons/non-veg-icon.svg" height="20px">
                                            }
                                            @if (menuitems.ItemType == 3)
                                            {
                                                <img src="~/images/icons/vegan-icon.svg" height=" 20px">
                                            }

                                        </td>
                                        <td>@menuitems.Rate</td>
                                        <td>@menuitems.Quantity</td>
                                        <td>
                                            <div class="form-check form-switch toggleSwitch">
                                                <input class="form-check-input" type="checkbox" role="switch"
                                                    id="flexSwitchCheckChecked" @(menuitems.Available ? "checked" : "")>
                                            </div>
                                        </td>
                                        <td>
                                            <div>
                                                <button class="menubtn loadEditModalBtn" data-id="@menuitems.Id"><i
                                                        class="bi bi-pen"></i></button>
                                                <button class="menubtn" data-bs-toggle="modal" data-bs-target="#deleteitem" data-id="@menuitems.Id"><i
                                                        class="bi bi-trash"></i></button>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }




                        </tbody>
                    </table>
                </div> *@
                <div id="menuItemsContainer">
                    <!-- The table will be loaded here -->
                </div>

                <div class="customPagination d-flex flex-column align-items-center mt-1">
                    <div class="d-flex align-items-center w-100 justify-content-end mb-2">
                        <div class="d-flex align-items-center me-auto me-sm-3">
                            <div class="me-sm-2" style="min-width: 7rem;">Items per page:</div>
                            <select class="form-select">
                                <option value="5" selected>5</option>
                                <option value="10">10</option>
                                <option value="15">15</option>
                            </select>
                        </div>
                        <div class="d-none d-sm-block me-sm-2">Showing 1-5 of 13</div>
                        <div>
                            <button id="prePageBtn" class="me-2">
                                <i class="bi bi-chevron-left"></i>
                            </button>
                            <button id="nextPageBtn">
                                <i class="bi bi-chevron-right"></i>
                            </button>
                        </div>
                    </div>
                    <div class="d-block d-sm-none">Showing 1-5 of 13</div>
                </div>
            </div>
        </div>
    </div>
</div>



<!-- model of add catogory -->

<div class="modal fade" style="margin-top: 13%;" id="addCategoryModal" tabindex="-1" aria-labelledby="exampleModalLabel"
    aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Add Category</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="form">
                    <form asp-controller="Category" asp-action="addcategory" method="post" class="form">
                        <input type="text" asp-for="Category" class="form-control" placeholder="Name"
                            style="margin-bottom: 20px;">
                        <span asp-validation-for="Category"></span>
                        <input type="text" asp-for="Description" class="form-control" placeholder="Description">
                        <div class="modal-footer">

                            <button type="submit" class="btn btn-primary">Save</button>
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        </div>
                    </form>

                </div>
            </div>

        </div>
    </div>
</div>

<div id="modalContainer"></div>


@* <!-- Add new item modal start -->
<div class="modal fade" id="addItemModal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
    aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header border-bottom-0">
                <h1 class="modal-title fs-5" id="staticBackdropLabel" style="color: #727b83;">Add New Menu Item</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body p-0 p-md-2">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-12 col-lg-8">
                            <div class="row row-cols-1 row-cols-lg-2 g-2 mb-2">
                                <div class="col">
                                    <div class="form-floating">
                                        <select class="form-select" id="category">
                                            <option value="1" selected>Sandwich</option>
                                            <option value="2">Pasta</option>
                                            <option value="3">Salads</option>
                                        </select>
                                        <label for="category">Categories*</label>
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="form-floating">
                                        <input type="text" class="form-control" id="name" placeholder="Name">
                                        <label for="name">Name*</label>
                                    </div>
                                </div>
                            </div>
                            <div class="row row-cols-2 row-cols-lg-3 row-cols-xl-4 g-2 mb-2">
                                <div class="col">
                                    <div class="form-floating">
                                        <select class="form-select" id="type">
                                            <option selected>Select a type</option>
                                            <option value="1">Veg</option>
                                            <option value="2">Non-veg</option>
                                            <option value="3">Vegan</option>
                                        </select>
                                        <label for="type">Item Type*</label>
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="form-floating">
                                        <input type="text" class="form-control" id="rate" placeholder="Rate" value="0">
                                        <label for="rate">Rate*</label>
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="form-floating">
                                        <input type="text" class="form-control" id="quantity" placeholder="Quantity"
                                            value="0">
                                        <label for="quantity">Quantity*</label>
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="form-floating">
                                        <select class="form-select" id="unit">
                                            <option selected>Select a unit</option>
                                            <option value="1">Liter</option>
                                            <option value="2">Kg</option>
                                            <option value="3">Piece</option>
                                        </select>
                                        <label for="unit">Unit*</label>
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="d-flex align-items-center h-100">
                                        <div class="form-check form-switch toggleSwitch me-2">
                                            <input class="form-check-input border-0" style="padding: 1rem 1.8rem;"
                                                type="checkbox" role="switch" id="flexSwitchCheckChecked">
                                        </div>
                                        Available
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="d-flex align-items-center h-100">
                                        <div class="form-check form-switch toggleSwitch me-2">
                                            <input class="form-check-input border-0" style="padding: 1rem 1.8rem;"
                                                type="checkbox" role="switch" id="flexSwitchCheckChecked">
                                        </div>
                                        DefaultTax
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="form-floating">
                                        <input type="text" class="form-control" id="tax" placeholder="Tax Percentage"
                                            value="0">
                                        <label for="tax">Tax Percentage</label>
                                    </div>
                                </div>
                                <div class="col">
                                    <div class="form-floating">
                                        <input type="text" class="form-control" id="shortCode" placeholder="Short Code"
                                            value="0">
                                        <label for="shortCode">Short Code</label>
                                    </div>
                                </div>
                            </div>
                            <div class="row row-cols-1 g-2">
                                <div class="col">
                                    <div class="form-floating">
                                        <textarea class="form-control" placeholder="Description" id="description"
                                            style="height: 100px"></textarea>
                                        <label for="description">Description</label>
                                    </div>
                                </div>
                                <div class="col">
                                    <input class=" d-none form-control" type="file" id="imageUpload" required>
                                    <label class="uploadFile" for="imageUpload">
                                        <div class="d-flex justify-content-center align-items-center">
                                            <img class="d-none" src="#" alt="Image Preview" id="imagePreview"
                                                height="75px" width="75px">
                                            <div class="d-flex align-items-center justify-content-center">
                                                <i class="bi bi-cloud-arrow-up-fill fs-4 me-2"></i>
                                                <div class="">Drag and Drop or Browse files</div>
                                            </div>
                                        </div>
                                        <div id="fileName" class="ms-3"></div>
                                    </label>
                                </div>
                            </div>
                        </div>
                        <div class="cols-12 col-lg-4 mt-2 mt-lg-0">
                            <div class="border-1 rounded-2 w-100 h-100 p-3" style="background-color: #fdf6e8;">
                                <select class="form-select" id="category">
                                    <option selected>Select Modifier Group(s)</option>
                                    <option value="1">s1</option>
                                    <option value="2">s3</option>
                                </select>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="modal-footer border-top-0 d-flex justify-content-end align-items-center">
                <button type="button" class="btn btn-primary">Save</button>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No</button>
            </div>
        </div>
    </div>
</div>
<!-- Add new item modal end --> *@




<!-- Delete Item -->
<div class="modal fade" style="margin-top: 13%;" id="deleteitem" tabindex="-1" aria-labelledby="deleteitem"
    aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title fs-5" id="exampleModalLabel">Delete Confirmation</h3>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body"
                style="display: flex;flex-direction: column; align-items: center;justify-content: center;">

                <div style="color: #ffc409;">
                    <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" fill="currentColor"
                        class="bi bi-exclamation-triangle-fill" viewBox="0 0 16 16">
                        <path
                            d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5m.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2" />
                    </svg>
                </div>
                <div>
                    <p>are you sure you want to delete Item?</p>
                </div>
                <div class="modal-footer">
                    <form asp-controller="Category" asp-action="deleteitem" method="post">
                        <input type="hidden" id="itemId" name="id" />
                        <button type="submit" class="btn btn-primary">Yes</button>
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No</button>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>





@section Scripts {

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    @* <script src="https://code.jquery.com/jquery-1.12.4.min.js"></script> *@



    <script>
        document.getElementById('selectAllCheckbox').addEventListener('change', function () {
            let checkboxes = document.querySelectorAll('.checkboxes');
            checkboxes.forEach(function (checkbox) {
                checkbox.checked = this.checked;
            }, this);
        });
    </script>




    <script>
        // When the delete button is clicked
        document.addEventListener('DOMContentLoaded', function () {
            var deleteButtons = document.querySelectorAll('.menubtn[data-bs-target="#delete"]');

            deleteButtons.forEach(function (button) {
                button.addEventListener('click', function () {
                    // Get the CategoryId from the data-id attribute of the button
                    var categoryId = this.getAttribute('data-id');

                    // Set the value of the hidden input field in the modal
                    document.getElementById('categoryId').value = categoryId;
                });
            });
        });
    </script>

    <script>
        // Ensure the DOM is fully loaded before executing the script
        document.addEventListener('DOMContentLoaded', function () {
            // Select all edit buttons
            var editButtons = document.querySelectorAll('[data-bs-target="#editCategoryModal"]');

            // Add click event to each edit button
            editButtons.forEach(function (button) {
                button.addEventListener('click', function () {
                    // Get the data attributes of the clicked button
                    var categoryId = this.getAttribute('data-id');
                    var categoryName = this.getAttribute('data-category-name');
                    var categoryDescription = this.getAttribute('data-description');

                    // Set the values in the modal
                    document.getElementById('CategoryId').value = categoryId;
                    document.getElementById('CategoryName').value = categoryName;
                    document.getElementById('Description').value = categoryDescription;
                });
            });
        });
    </script>









    @* <script>
        $(document).ready(function () {
            // Load the categories on page load
            loadCategories();

            function loadCategories() {
                $.ajax({
                    url: '@Url.Action("GetCategories", "Category")', // Use the correct controller and action name
                    type: 'GET',
                    success: function (response) {
                        $('#categoryListContainer').html(response); // Inject the partial view into the container
                    },
                    error: function () {
                        alert('Error loading categories');
                    }
                });
            }
        });
    </script> *@

    <script>
        $(document).ready(function () {

            // When the 'Add New Item' button is clicked
            $('#loadModalBtn').on('click', function () {
                $.ajax({
                    url: '@Url.Action("AddItemModal", "Category")', // Call the action in the Menu controller
                    type: 'GET',
                    success: function (response) {
                        // Insert the partial view into the modalContainer
                        $('#modalContainer').html(response);

                        // Optionally, if you want to show the modal after loading
                        $('#addItemModal').modal('show');
                    },
                    error: function () {
                        alert("An error occurred while loading the modal.");
                    }
                });
            });
        });
    </script>

    <script>
        $(document).ready(function () {
            // When the 'Edit' button is clicked
            $('.loadEditModalBtn').on('click', function () {
                var itemId = $(this).data('id'); // Get the item id from the button's data-id attribute

                $.ajax({
                    url: '@Url.Action("EditItemModal", "Category")',  // Call the action in the Category controller
                    type: 'GET',
                    data: { id: itemId },  // Pass the itemId as part of the GET request
                    success: function (response) {
                        // Insert the partial view into the modalContainer
                        $('#modalContainer').html(response);

                        // Show the modal after loading
                        $('#editItemModal').modal('show');
                    },
                    error: function () {
                        alert("An error occurred while loading the modal.");
                    }
                });
            });
        });
    </script>


    <script>
        // When the delete button is clicked
        document.addEventListener('DOMContentLoaded', function () {
            var deleteButtons = document.querySelectorAll('.menubtn[data-bs-target="#deleteitem"]');

            deleteButtons.forEach(function (button) {
                button.addEventListener('click', function () {
                    // Get the CategoryId from the data-id attribute of the button
                    var itemId = this.getAttribute('data-id');
                    console.log(itemId);
                    // Set the value of the hidden input field in the modal
                    document.getElementById('itemId').value = itemId;
                });
            });
        });
    </script>




    <script>
        $(document).ready(function () {
            console.log("hello1");
            // 1. Automatically load the menu items for the first category when the page loads
            var firstCategoryId = $('a[data-category-id]').first().data('category-id'); // Get the ID of the first category
            loadMenuItems(firstCategoryId);  // Call the function to load the menu items for the first category
            console.log(firstCategoryId);
            // 2. Event listener for clicking on category links
            $('a[data-category-id]').click(function () {
                var categoryId = $(this).data('category-id'); // Get category ID from clicked link
                loadMenuItems(categoryId);  // Load menu items for the clicked category
            });


            // Function to load menu items via AJAX
            function loadMenuItems(categoryId) {
                console.log(categoryId);
                $.ajax({
                    url: '@Url.Action("GetMenuItemsTable", "Category")', // Your controller and action to fetch filtered items
                    type: 'GET',
                    data: { id: categoryId },  // Pass the category ID
                    success: function (result) {
                        $('#menuItemsContainer').html(result);  // Load the result (filtered menu items) into the container
                    },
                    error: function (xhr, status, error) {
                        console.log("Error: " + error);  // Handle any errors
                    }
                });
            }
        });
    </script>

    @* <script>
        $(document).ready(function () {

            var firstCategoryId = $('a[data-category-id]').first().data('category-id'); // Get the ID of the first category
            loadMenuItems(firstCategoryId);  // Call the function to load the menu items for the first category

            $('a[data-category-id]').click(function () {
                var categoryId = $(this).data('category-id');
                // Trigger AJAX call when the page is ready (or you could trigger it based on a specific event)
                $.ajax({
                    url: '@Url.Action("GetMenuItemsTable", "Category")', // Adjust this to your controller and action
                    type: 'GET',
                    data: { id: categoryId }, // Pass the category ID
                    success: function (result) {
                        // Load the result (table) into the div
                        $('#menuItemsContainer').html(result);
                    },
                    error: function (xhr, status, error) {
                        console.log("Error: " + error);
                    }
                });
            });
    </script> *@


}
